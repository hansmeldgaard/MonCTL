# Extra Python packages installed into the Superset container at startup.
# Picked up automatically by the official image's docker-bootstrap.sh.

# PostgreSQL driver for the metadata DB
psycopg2-binary==2.9.9

# ClickHouse driver (SQLAlchemy dialect used by the MonCTL DB connection)
clickhouse-connect>=0.8.0

# OAuth client — needed for AUTH_TYPE = AUTH_OAUTH (Flask-AppBuilder imports
# this lazily, so it isn't in the base image).
authlib>=1.3.0
