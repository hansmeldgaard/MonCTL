{
  "monctl_pack": "1.0",
  "pack_uid": "snmp-uptime",
  "name": "SNMP Device Uptime",
  "version": "1.0.0",
  "description": "Monitors device uptime via SNMP. Prefers snmpEngineTime (wraps at ~68 years), falls back to sysUpTime (wraps at ~497 days). Includes reboot detection alert.",
  "author": "MonCTL",
  "changelog": "v1.0.0 - Initial release: SNMP uptime polling with snmpEngineTime/sysUpTime fallback, reboot detection alert, counter wrap warning",
  "exported_at": null,
  "contents": {
    "apps": [
      {
        "name": "snmp_uptime",
        "description": "Monitors device uptime via SNMP. Prefers snmpEngineTime (seconds, wraps ~68y) over sysUpTime (centiseconds, wraps ~497d). Writes uptime_seconds and uptime_days to the performance table.",
        "app_type": "script",
        "target_table": "performance",
        "config_schema": {
          "type": "object",
          "properties": {}
        },
        "connector_bindings": [
          {
            "alias": "snmp",
            "connector_name": "snmp"
          }
        ],
        "threshold_variables": [
          {
            "name": "reboot_threshold",
            "default_value": 300,
            "display_name": "Reboot Detection Threshold",
            "unit": "seconds",
            "description": "Alert fires when uptime is below this value (indicates recent reboot). Default: 300s (5 minutes)."
          }
        ],
        "alert_definitions": [
          {
            "name": "Device Rebooted",
            "description": "Device uptime is below the reboot threshold, indicating a recent reboot or restart.",
            "expression": "last(uptime_seconds) < $reboot_threshold",
            "window": "5m",
            "severity": "warning",
            "enabled": true,
            "message_template": "{device_name} rebooted — uptime is {value} seconds"
          },
          {
            "name": "Uptime Counter Wrap Warning",
            "description": "sysUpTime counter is approaching the 497-day wrap limit. The reported uptime value may reset to zero. Only relevant when snmpEngineTime is unavailable.",
            "expression": "last(uptime_days) > 490",
            "window": "1h",
            "severity": "info",
            "enabled": false,
            "message_template": "{device_name} uptime counter approaching 497-day wrap limit ({value} days)"
          }
        ],
        "versions": [
          {
            "version": "1.0.0",
            "source_code": "\"\"\"SNMP device uptime monitor — polls snmpEngineTime or sysUpTime for uptime tracking.\n\nTries snmpEngineTime (1.3.6.1.6.3.10.2.1.3.0) first — it reports seconds and\nwraps at ~68 years.  Falls back to sysUpTime.0 (1.3.6.1.2.1.1.3.0) which\nreports centiseconds and wraps at ~497 days.\n\nConnector binding required:\n    alias \"snmp\" → SNMP connector with credential providing auth parameters\n\nWrites to: performance table\n    uptime_seconds   Device uptime in seconds (gauge)\n    uptime_days      Device uptime in days (gauge)\n\"\"\"\n\nfrom __future__ import annotations\n\nimport time\n\nfrom monctl_collector.jobs.models import PollContext, PollResult\nfrom monctl_collector.polling.base import BasePoller\n\n# snmpEngineTime — seconds since SNMP engine boot, wraps at ~68 years\n_OID_ENGINE_TIME = \"1.3.6.1.6.3.10.2.1.3.0\"\n# sysUpTime — centiseconds since last re-init, wraps at ~497 days\n_OID_SYS_UPTIME = \"1.3.6.1.2.1.1.3.0\"\n\n\ndef _parse_timeticks(raw: object) -> float | None:\n    \"\"\"Parse an SNMP TimeTicks / integer value to a raw numeric value.\n\n    Handles both plain integers and the ``TimeTicks: (12345)`` string\n    representation that some SNMP libraries return.\n    \"\"\"\n    if raw is None:\n        return None\n    s = str(raw).strip()\n    # Strip \"TimeTicks: (nnn)\" wrapper if present\n    if \"(\" in s and \")\" in s:\n        s = s[s.index(\"(\") + 1 : s.index(\")\")]\n    try:\n        return float(s)\n    except (ValueError, TypeError):\n        return None\n\n\nclass Poller(BasePoller):\n    \"\"\"SNMP uptime poller — prefers snmpEngineTime, falls back to sysUpTime.\"\"\"\n\n    async def poll(self, context: PollContext) -> PollResult:\n        host = context.device_host or context.parameters.get(\"host\", \"localhost\")\n\n        snmp = context.connectors.get(\"snmp\")\n        if snmp is None:\n            return PollResult(\n                job_id=context.job.job_id,\n                device_id=context.job.device_id,\n                collector_node=context.node_id,\n                timestamp=time.time(),\n                metrics=[],\n                config_data=None,\n                status=\"error\",\n                reachable=False,\n                error_message=\"No SNMP connector bound (expected alias 'snmp')\",\n                execution_time_ms=0,\n            )\n\n        start = time.monotonic()\n        try:\n            # Try both OIDs in one GET for efficiency\n            result = await snmp.get([_OID_ENGINE_TIME, _OID_SYS_UPTIME])\n            elapsed_ms = (time.monotonic() - start) * 1000\n\n            uptime_seconds: float | None = None\n            source = \"\"\n\n            # Prefer snmpEngineTime (already in seconds)\n            engine_val = _parse_timeticks(result.get(_OID_ENGINE_TIME))\n            if engine_val is not None and engine_val > 0:\n                uptime_seconds = engine_val\n                source = \"snmpEngineTime\"\n\n            # Fallback to sysUpTime (centiseconds → seconds)\n            if uptime_seconds is None:\n                sys_val = _parse_timeticks(result.get(_OID_SYS_UPTIME))\n                if sys_val is not None and sys_val >= 0:\n                    uptime_seconds = sys_val / 100.0\n                    source = \"sysUpTime\"\n\n            if uptime_seconds is None:\n                return PollResult(\n                    job_id=context.job.job_id,\n                    device_id=context.job.device_id,\n                    collector_node=context.node_id,\n                    timestamp=time.time(),\n                    metrics=[],\n                    config_data=None,\n                    status=\"critical\",\n                    reachable=True,\n                    error_message=f\"Could not parse uptime from {host}\",\n                    execution_time_ms=int(elapsed_ms),\n                )\n\n            uptime_days = uptime_seconds / 86400.0\n\n            return PollResult(\n                job_id=context.job.job_id,\n                device_id=context.job.device_id,\n                collector_node=context.node_id,\n                timestamp=time.time(),\n                metrics=[\n                    {\n                        \"name\": \"uptime_seconds\",\n                        \"value\": uptime_seconds,\n                        \"labels\": {\"host\": host, \"source\": source},\n                        \"unit\": \"seconds\",\n                        \"component\": \"uptime\",\n                        \"component_type\": \"system\",\n                    },\n                    {\n                        \"name\": \"uptime_days\",\n                        \"value\": round(uptime_days, 2),\n                        \"labels\": {\"host\": host, \"source\": source},\n                        \"unit\": \"days\",\n                        \"component\": \"uptime\",\n                        \"component_type\": \"system\",\n                    },\n                ],\n                config_data=None,\n                status=\"ok\",\n                reachable=True,\n                error_message=None,\n                execution_time_ms=int(elapsed_ms),\n            )\n\n        except Exception as exc:\n            elapsed_ms = (time.monotonic() - start) * 1000\n            return PollResult(\n                job_id=context.job.job_id,\n                device_id=context.job.device_id,\n                collector_node=context.node_id,\n                timestamp=time.time(),\n                metrics=[],\n                config_data=None,\n                status=\"critical\",\n                reachable=False,\n                error_message=f\"SNMP uptime check failed — {host}: {type(exc).__name__}: {exc}\",\n                execution_time_ms=int(elapsed_ms),\n            )\n",
            "requirements": [],
            "entry_class": "Poller",
            "is_latest": true
          }
        ]
      }
    ],
    "snmp_oids": [
      {
        "name": "snmpEngineTime",
        "oid": "1.3.6.1.6.3.10.2.1.3.0",
        "description": "SNMP engine uptime in seconds since last re-initialization. Wraps at ~68 years (2^31 seconds). Preferred source for device uptime."
      },
      {
        "name": "sysUpTime",
        "oid": "1.3.6.1.2.1.1.3.0",
        "description": "System uptime in hundredths of a second (TimeTicks) since last re-initialization. Wraps at ~497 days (2^32 centiseconds). Fallback source."
      }
    ]
  }
}
