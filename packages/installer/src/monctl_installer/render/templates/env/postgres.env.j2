# Postgres on {{ host.name }} — rendered by monctl_ctl
# Secrets come from ./secrets.env (fed by monctl_ctl init); do not commit this file.
PG_PASSWORD=${PG_PASSWORD}
{% if plan.pg_ha %}
PG_REPL_PASSWORD=${PG_REPL_PASSWORD}
{% endif %}
